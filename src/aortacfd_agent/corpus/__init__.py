"""RAG corpus for the LiteratureAgent.

The corpus itself (PDFs) is not committed to git — see
``src/aortacfd_agent/corpus/papers/README.md`` for the paper list and
how to fetch each one from its open-access source.
"""
